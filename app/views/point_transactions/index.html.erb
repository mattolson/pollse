<h1>Listing point_transactions</h1>

<table>
  <tr>
    <th>User</th>
    <th>Points</th>
    <th>Transaction type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @point_transactions.each do |point_transaction| %>
  <tr>
    <td><%= point_transaction.user %></td>
    <td><%= point_transaction.points %></td>
    <td><%= point_transaction.transaction_type %></td>
    <td><%= link_to 'Show', point_transaction %></td>
    <td><%= link_to 'Edit', edit_point_transaction_path(point_transaction) %></td>
    <td><%= link_to 'Destroy', point_transaction, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Point transaction', new_point_transaction_path %>
