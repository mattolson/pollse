<h1>All Open Polls</h1>
<div class="big-list">
	<ul>
	  <% @polls.each do |p| %>
	    <li>
				<span class="meta"><%= p.responses.size %> Responsess</span>
				<%= link_to p, p %>
			</li>
	  <% end %>
	</ul>
</div>
