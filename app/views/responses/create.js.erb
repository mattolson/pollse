$('#new_response').fadeOut('fast', function() {
  $("#poll_results_<%= @response.poll.id %>").html("<%=j render :partial => '/polls/results', :locals => {:poll => @response.poll} %>");
  $("#poll_results_<%= @response.poll.id %>").fadeIn('fast');
});
